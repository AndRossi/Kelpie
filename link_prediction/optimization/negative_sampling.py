import numpy

from dataset import Dataset


def corrupt(positive_samples: numpy.array,
            corrupt_rate: int,
            dataset: Dataset):

    negative_samples = []
    #for sample in positive_samples:
        # for i in range(corrupt_rate):
            # decidi a caso se replace head o tail
            # estrai un'entità a caso per rimpiazzare
            # while l'entità a caso non va bene perché in filter_out:
            #   riestrai
            # negative_samples.append(...)

    #return negative samples

